server {
    listen 80;
    server_name dadiai.cn www.dadiai.cn;

    # 日志
    access_log /var/log/nginx/dadiai.access.log;
    error_log  /var/log/nginx/dadiai.error.log;

    # 上传文件大小限制
    client_max_body_size 50M;

    # 反向代理到 FastAPI (uvicorn 8000端口)
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持（如需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置（视频生成任务可能较久）
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS 配置（用 certbot 申请证书后取消注释）
# server {
#     listen 443 ssl http2;
#     server_name dadiai.cn www.dadiai.cn;
#
#     ssl_certificate     /etc/letsencrypt/live/dadiai.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dadiai.cn/privkey.pem;
#
#     client_max_body_size 50M;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 600s;
#         proxy_send_timeout 600s;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
# }
#
# # HTTP -> HTTPS 重定向
# server {
#     listen 80;
#     server_name dadiai.cn www.dadiai.cn;
#     return 301 https://$host$request_uri;
# }
